[package]
name = "keyvalue-filesystem"
description = "A wash dev plugin that implements `wasi:keyvalue` using `wasi:filesystem`"
edition = "2021"
version = "0.1.0"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1.0.98"
wit-bindgen = "0.43.0"
wasi = "0.14"
